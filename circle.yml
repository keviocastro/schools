## Customize the test machine
machine:
  services:
    - docker

compile:
  override:
    - docker-compose up -d

test:
  override:
    -  docker exec schools_web_1 scripts/wait-health.sh vendor/bin/phpunit